# Secrets — never commit
.env

# Python
.venv/
__pycache__/
*.pyc
.DS_Store

# Regenerable build artifacts (rebuilt by the pipeline; see README)
results/cache/
*.log

# Smoke-test outputs (from `python -m src pilot`)
*_pilot.jsonl

# Internal lab notes — never publish
HANDOFF*.md
BRIEFING*.md
